# Iteration 21 - Wake phrase phonetic coverage

1. [Conversation] ASR can render the final syllable as `诗`; `贾维诗` is now accepted.
2. [Conversation] ASR can render the final syllable as `史`; `贾维史` is now accepted.
3. [Conversation] ASR can render the first syllable as `甲`; `甲维斯` is now accepted.
4. [Conversation] ASR can combine `假` and `威`; `假威斯` is now accepted.
5. [Conversation] ASR can combine `加` and `威`; `加威斯` is now accepted.
6. [Conversation] ASR can render the middle syllable as `韦`; `贾韦斯` is now accepted.
7. [Conversation] ASR can render the final syllable as `思`; `加维思` is now accepted.
8. [Conversation] ASR can combine `佳` and `思`; `佳维思` is now accepted.
9. [Conversation] English ASR can emit `jarves`; that form is now accepted.
10. [Conversation] English ASR can emit `jarvice`; that form is now accepted.
